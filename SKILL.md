# AgentHQ Skill

Install AgentHQ with the one-line installer:

```bash
cd ~/.openclaw/workspace
curl -fsSL https://raw.githubusercontent.com/98kiran/agenthq/master/install.sh | bash
```

That script checks prerequisites, clones or updates the repo, installs Python dependencies, runs `setup.sh sqlite`, and starts AgentHQ with PM2.
